import { FilterClientListPipe } from './filter-client-list.pipe';

describe('FilterClientListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClientListPipe();
    expect(pipe).toBeTruthy();
  });
});
