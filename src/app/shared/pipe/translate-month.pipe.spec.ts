import { TranslateMonthPipe } from './translate-month.pipe';

describe('TranslateMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
