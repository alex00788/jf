import { FilterOrgPipe } from './filter-org.pipe';

describe('FilterOrgPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterOrgPipe();
    expect(pipe).toBeTruthy();
  });
});
