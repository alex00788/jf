import { ReductionAddressPipe } from './reduction-address.pipe';

describe('ReductionAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new ReductionAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
